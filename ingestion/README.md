# ingestion

Reserved for the Lenny's Podcast transcript ingestion pipeline.

Phase 1 (Foundation) deliberately contains no ingestion code: transcript
discovery, frontmatter parsing, chunking, embeddings and `pgvector` storage are
part of the Knowledge Base & Ingestion phase.

This directory exists so the repository layout matches `docs/architecture.md`.
